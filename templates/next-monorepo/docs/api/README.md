# API Notes

This template does not include a separate backend service.

If you add route handlers inside `apps/web`, document:

- the route path
- the expected request shape
- the response shape
- any auth or caching assumptions
