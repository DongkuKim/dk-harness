# API Contract

Current Axum endpoints:

- `GET /`: returns plain text `axum server`
- `GET /health`: returns plain text `ok`

When changing handlers, update this document with:

- route path and method
- request requirements
- response body and status codes
- compatibility notes for callers
