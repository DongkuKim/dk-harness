set shell := ["bash", "-cu"]

install:
  mise install
  pnpm install
  (cd apps/fast-api && uv sync --group dev)
  pnpm exec playwright install chromium

lint:
  pnpm --filter web lint
  pnpm --dir packages/ui exec depcruise --config dependency-cruiser.cjs src
  pnpm --filter fast-api lint

typecheck:
  pnpm --filter web typecheck
  pnpm --filter fast-api typecheck

test:
  pnpm --filter web test
  pnpm --filter fast-api test

ux:
  pnpm --filter web ux

supply-chain:
  gitleaks dir . --config .gitleaks.toml
  osv-scanner scan source -r .
  pnpm audit --audit-level=high
  pnpm --filter fast-api supply-chain

ci: lint typecheck test ux supply-chain
